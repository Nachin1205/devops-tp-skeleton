{
  "name": "devops-tp-skeleton",
  "version": "1.0.0",
  "description": "Starter para TP Integrador DevOps: Express + Jest + Docker + CI (GitHub Actions)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "lint": "eslint . --ext .js --max-warnings=0"
  },
  "dependencies": {
    "@sentry/node": "^10.20.0",
    "@sentry/profiling-node": "^10.21.0",
    "express": "^4.19.2",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "eslint": "8.57.1",
    "supertest": "7.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
