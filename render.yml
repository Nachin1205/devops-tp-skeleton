services:
  - type: web
    name: devops-tp-skeleton
    env: image
    image: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/devops-tp:latest
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
